{
  "name": "lif-sizzle",
  "version": "1.0.0",
  "description": "LIF — Love It Forward sizzle reel (static).",
  "private": true,
  "scripts": {
    "start": "serve -s . -l ${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
